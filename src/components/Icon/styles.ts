/**
 * Icon component styles loader
 * Import this to load only Icon styles (not the icon fonts themselves)
 * Use loadIconFont() or loadIconSvg() from the main package to load icon assets
 */
import './Icon.scss';

export default function loadIconStyles(): void {
	// Styles are loaded via the import above
	// This function exists for explicit API and future enhancement
}
