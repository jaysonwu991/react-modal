/**
 * Button component styles loader
 * Import this to load only Button styles
 */
import './Button.scss';

export default function loadButtonStyles(): void {
	// Styles are loaded via the import above
	// This function exists for explicit API and future enhancement
}
