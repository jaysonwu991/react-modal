/**
 * Calendar component styles loader
 * Import this to load only Calendar styles
 */
import './Calendar.scss';

export default function loadCalendarStyles(): void {
	// Styles are loaded via the import above
	// This function exists for explicit API and future enhancement
}
