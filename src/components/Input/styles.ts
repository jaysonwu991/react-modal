/**
 * Input component styles loader
 * Import this to load only Input styles
 */
import './Input.scss';

export default function loadInputStyles(): void {
	// Styles are loaded via the import above
	// This function exists for explicit API and future enhancement
}
