/**
 * Modal component styles loader
 * Import this to load only Modal styles
 */
import './Modal.scss';

export default function loadModalStyles(): void {
	// Styles are loaded via the import above
	// This function exists for explicit API and future enhancement
}
